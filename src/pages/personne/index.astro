---
import { getCollection } from "astro:content"
import Layout from "../../layouts/Layout.astro";

const personne = await getCollection("personne");

---

<Layout pageTitle="Liste de Personne">
    <pre>
        {JSON.stringify(personne, null, 2)}
    </pre>
  
</Layout>
